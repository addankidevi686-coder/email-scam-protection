# This directory stores the trained ML model files.
# Run `python train_model.py` from the project root to generate:
#   - spam_model.pkl
#   - vectorizer.pkl
#
# Without these files, the system falls back to rule-based detection.
